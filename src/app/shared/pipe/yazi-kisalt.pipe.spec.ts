import { YaziKisaltPipe } from './yazi-kisalt.pipe';

describe('YaziKisaltPipe', () => {
  it('create an instance', () => {
    const pipe = new YaziKisaltPipe();
    expect(pipe).toBeTruthy();
  });
});
